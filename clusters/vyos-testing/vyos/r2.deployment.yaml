apiVersion: apps/v1
kind: Deployment
metadata:
  name: R2
  namespace: vyos-testing
spec:
  selector:
    matchLabels:
      app: R2
  template:
    metadata:
      labels:
        app: R2
      annotations:
        networkservicemesh.io: kernel://vyos-testing/nsm-1
    spec:
      containers:
        - name: R2
          image: vyos/vyos-build:current
          resources:
            limits:
              memory: "128Mi"
              cpu: "500m"
          # ports:
          #   - containerPort: <Port>
