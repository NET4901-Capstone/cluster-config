# cluster-config
This repository contains the cluster state for the various clusters used throughout the project. Each directory within the `clusters` folder represents an individual cluster configuration. Some common resources such as helm charts and applications which are re-used between apps are located in `helm_repositories` and `common`, repsectively.
